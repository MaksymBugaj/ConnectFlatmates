<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.connect.connectflatmates">

    <dist:module dist:instant="true" />

    <application
        android:name=".ConnectFlatmates"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:theme">
        <activity
            android:name=".ui.DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/NoActionBar"></activity>
        <activity
            android:name=".ui.menu.NavigationDrawerHoldingActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar.Transparent" />
        <activity android:name=".ui.menu.home.BottomNavHolderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>