<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="">

    <group android:checkableBehavior="single">
        <item android:id="@+id/myProfile"
            android:icon="@drawable/profile"
            android:title="My Profile"/>

        <item android:id="@+id/gloryHole"
            android:icon="@drawable/ic_baseline_star_border_24"
            android:title="Add people"/>

    </group>

    <group android:checkableBehavior="single">
        <item android:id="@+id/home_Act"
            android:icon="@drawable/home"
            android:title="Home Activities"/>

        <item android:id="@+id/home_Act_add"
            android:icon="@drawable/add_button"
            android:title="Add home activities"/>

        <item android:id="@+id/home_Act_list"
            android:icon="@drawable/ic_list"
            android:title="List of act"/>
    </group>
</menu>